

    // Math.random() gera aleatoriamente um número >=0 e <1.
    // cada vez que o comando é executado um número >=0 e <1 é sorteado.

    function executar() {
        div_result.innerHTML += `${Math.random()} / `;
    }

